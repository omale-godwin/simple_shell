#include "simpleShell.h"
#include <stdio.h>

int main(void)
{
	int mode1 = 100;
	int mode2 = 50;
	int result = mode1 * mode2;

	printf("The cal sum of %d and %d is %d\n", mode1, mode2, result);

	return (0);
}
